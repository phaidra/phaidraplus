{{!
{
  "name" : "Node GexF Template",
  "description" : "",
}
}}
<node id="{{id}}" label="{{label}}">
	<attvalues>
		<attvalue for="type" value="{{type}}"></attvalue>
		<attvalue for="type" value="{{preview}}"></attvalue>
	</attvalues>
    <viz:size value="{{size}}"></viz:size>
    <viz:position x="{{x}}" y="{{y}}" z="0.0"></viz:position>
    <viz:color r="{{r}}" g="{{g}}" b="{{b}}"></viz:color>
</node>